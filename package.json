{
  "name": "conviction-forge-worker",
  "version": "0.1.0",
  "private": true,
  "description": "Isolated coding worker for ioradsun/belief-compass. Implements the Conviction Forge Worker contract.",
  "type": "module",
  "module": "src/index.ts",
  "engines": {
    "bun": ">=1.1.0"
  },
  "scripts": {
    "start": "bun run src/index.ts",
    "dev": "bun --watch src/index.ts",
    "typecheck": "tsc --noEmit",
    "test": "bun test",
    "contract": "bun run test/contract.test.ts"
  },
  "devDependencies": {
    "@types/bun": "^1.1.14",
    "typescript": "^5.8.3"
  }
}
